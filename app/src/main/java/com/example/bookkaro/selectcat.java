package com.example.bookkaro;import android.content.Intent;import android.os.Bundle;import android.view.View;import android.widget.Button;import android.widget.CheckBox;import androidx.activity.EdgeToEdge;import androidx.appcompat.app.AppCompatActivity;import androidx.core.graphics.Insets;import androidx.core.view.ViewCompat;import androidx.core.view.WindowInsetsCompat;public class selectcat extends AppCompatActivity {Button button;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        EdgeToEdge.enable(this);        setContentView(R.layout.activity_selectcat);        button=findViewById(R.id.login_button);        button.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View view) {                CheckBox businessCheckBox = findViewById(R.id.checkBox3);                CheckBox childrenCheckBox = findViewById(R.id.checkBox4);                CheckBox computerCheckBox = findViewById(R.id.checkBox);                CheckBox dictionaryCheckBox = findViewById(R.id.checkBox1);                CheckBox dramaCheckBox = findViewById(R.id.checkBox2);                CheckBox cookingCheckBox = findViewById(R.id.checkBox0);                CheckBox fictionCheckBox = findViewById(R.id.checkBox9);                CheckBox encyclopediaCheckBox = findViewById(R.id.checkBox12);                // Create a string of selected categories                StringBuilder categories = new StringBuilder();                if (businessCheckBox.isChecked()) categories.append("Business, ");                if (childrenCheckBox.isChecked()) categories.append("Children's Book, ");                if (computerCheckBox.isChecked()) categories.append("Computer Science, ");                if (dictionaryCheckBox.isChecked()) categories.append("Dictionary, ");                if (dramaCheckBox.isChecked()) categories.append("Drama, ");                if (cookingCheckBox.isChecked()) categories.append("Cooking, ");                if (fictionCheckBox.isChecked()) categories.append("Fiction, ");                if (encyclopediaCheckBox.isChecked()) categories.append("Encyclopedia, ");                // Remove the trailing comma and space                if (categories.length() > 0) {                    categories.setLength(categories.length() - 2);                }                // Convert categories to string                String selectedCategories = categories.toString();                // Call categoriesData method to insert selected categories into the database                DBHelperlogin db = new DBHelperlogin(selectcat.this);                boolean isInserted = db.categoriesData(selectedCategories); // Pass the actual selectedCategories                Intent i=new Intent(selectcat.this,mainhomepage.class);                startActivity(i);            }        });        ViewCompat.setOnApplyWindowInsetsListener(findViewById(R.id.main), (v, insets) -> {            Insets systemBars = insets.getInsets(WindowInsetsCompat.Type.systemBars());            v.setPadding(systemBars.left, systemBars.top, systemBars.right, systemBars.bottom);            return insets;        });    }}